# TLS-обёртка (Фаза 5). Caddy сам получает и продлевает сертификат Let's Encrypt
# для DOMAIN и проксирует на сервис web. Требуется реальный домен, указывающий
# на этот сервер, и открытые порты 80/443.
{$DOMAIN} {
	reverse_proxy web:80
}
